package com.example.todo.logging.model;

/**
 * Enum representing different log levels based on severity
 */
public enum LogLevel {
    TRACE,
    DEBUG,
    INFO,
    WARN,
    ERROR,
    FATAL
}